-- PSICOSENATICO | Scam Drive World
-- Mantem o scanner original e adiciona minimizar/restaurar na interface criada pelo alvo.

local url = "https://raw.githubusercontent.com/Psicosenatico/Drive-World/main/Tools/ScamDriveWorld_Minimized.lua"
local source = game:HttpGet(url)
local fn, err = loadstring(source)
if not fn then
    error("Scam wrapper compile error: " .. tostring(err))
end
return fn()